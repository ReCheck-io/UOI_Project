spring:
 data:
  neo4j:
    username: neo4j
    password: test

springdoc:
 api-docs:
  path: "/api-docs"
 swagger-ui:
  path: "/swagger"
  operationsSorter: method

server:
 forward-headers-strategy: framework