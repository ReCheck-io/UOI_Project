spring:
 profiles:
   active: '@spring.profile@'
 data:
  neo4j:
    username: neo4j
    password: test
 jpa:
   hibernate:
     ddl-auto: update
     show-sql: true

springdoc:
 api-docs:
  path: "/api-docs"
 swagger-ui:
  path: "/swagger"
  operationsSorter: method

server:
 forward-headers-strategy: framework

#logging:
#  level:
#    root: DEBUG