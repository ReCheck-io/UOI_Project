spring:
 data:
  neo4j:
#    uri: neo4j+s://uoi-admin.recheck.io:7474
    username: neo4j
    password: test

springdoc:
 api-docs:
  path: "/api-docs"
 swagger-ui:
  path: "/swagger"
  operationsSorter: method

server:
 forward-headers-strategy: framework

#logging:
#  level:
#    root: DEBUG